myFun() {
    return
    0;
}

main() {         // You may assume there are no parameters to main.
    return
        3,
        putint(getint() + (3 - 2)),
        putchar(10),
        myFun(),
        0;
}
